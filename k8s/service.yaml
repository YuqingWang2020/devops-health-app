apiVersion: v1
kind: Service
metadata:
  name: health-api
  labels:
    app: health-api
spec:
  selector:
    app: health-api        # match Deployment label
  ports:
  - name: http             #  for ServiceMonitor 
    port: 8080
    targetPort: 8080
